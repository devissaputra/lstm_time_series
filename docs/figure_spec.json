{
  "question": "Can a recurrent model improve weekly CO₂ forecasts?",
  "data": "Mauna Loa weekly CO₂ from statsmodels; 24-week input windows.",
  "method": "Past-only forward fill; training-only normalization; chronological holdout; persistence, ridge and LSTM.",
  "formula": "RMSE = sqrt(mean((forecast-observed)^2)); MAE = mean(|forecast-observed|).",
  "interpretation": "MAE and RMSE are calculated after reversing training-fitted normalization and are reported in ppm. Later test predictions use earlier observed test values, so this is one-step rolling evaluation rather than a recursive forecast from one origin. One seed, fixed hyperparameters and one test era limit generalization.",
  "status": "CORRECTED EXTERNAL-DATA RUN | GitHub Actions reproduction succeeded",
  "source": "results/metrics.json",
  "rows": [
    {
      "label": "persistence RMSE",
      "path": [
        "persistence",
        "rmse"
      ],
      "unit": "ppm ↓"
    },
    {
      "label": "persistence MAE",
      "path": [
        "persistence",
        "mae"
      ],
      "unit": "ppm ↓"
    },
    {
      "label": "ridge RMSE",
      "path": [
        "ridge",
        "rmse"
      ],
      "unit": "ppm ↓"
    },
    {
      "label": "ridge MAE",
      "path": [
        "ridge",
        "mae"
      ],
      "unit": "ppm ↓"
    },
    {
      "label": "lstm RMSE",
      "path": [
        "lstm",
        "rmse"
      ],
      "unit": "ppm ↓"
    },
    {
      "label": "lstm MAE",
      "path": [
        "lstm",
        "mae"
      ],
      "unit": "ppm ↓"
    }
  ],
  "table_title": "Corrected 20-epoch run: chronological test errors",
  "evidence_note": "Reproduced in GitHub Actions run 36235069744; seed 42; errors in ppm.",
  "finding": "This experiment compares persistence, ridge autoregression, and a compact LSTM for next-week Mauna Loa CO₂ forecasting. Missing weeks now use past-only forward filling, and the corrected 20-epoch experiment has been rerun successfully in GitHub Actions. On the 452-week chronological holdout, ridge achieves RMSE 0.4639 ppm, persistence 0.5135 ppm, and the LSTM 1.0751 ppm. The recurrent model therefore does not justify its added complexity in this fixed-seed setup."
}
